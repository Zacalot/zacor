[package]
name = "zr-expr"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Expression evaluator for predicate and value expressions on JSON records"

[dependencies]
serde_json = "1"
regex = "1"
